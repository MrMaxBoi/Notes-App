version: '3.8'

services:
  notes-app:
    build: .
    ports:
      - "5050:5050"
    environment:
      - SPRING_PROFILES_ACTIVE=production
      - SERVER_PORT=5050
    volumes:
      - ./database.sqlite:/app/database.sqlite
    restart: unless-stopped